import React from 'react';
import styled from 'styled-components';

const SectionContainer = styled.section`
	background-color: cornsilk;
	height: 20rem;
`;

const HomeHeroIntro: React.FC = () => (
	<SectionContainer />
);

export default HomeHeroIntro;
